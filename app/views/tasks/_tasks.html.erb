<ul class="media-list">
  <% tasks.each do |task| %>
    <% user = task.user %>
    <li class="media">
        <div>
          <p><%= link_to task.content, task_path(task.id) %></p>
        </div>
    </li>
  <% end %>
</ul>